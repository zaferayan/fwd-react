// GÖREV 1: Media type'ı
// buraya yaz


// GÖREV 2: formatDuration — tipleri ekle
function formatDuration(duration) {
  if (typeof duration === "number") {
    return `${duration} saniye`;
  }
  return duration;
}


// GÖREV 3: ContentCard — intersection type
// buraya yaz
